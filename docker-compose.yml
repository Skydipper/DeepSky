version: '3'
services:
  deepsky:
    image: deepsky
    build: .
    ports:
      - 8888:8888
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [ gpu ]

    volumes:
      - .:/tf/deepsky
